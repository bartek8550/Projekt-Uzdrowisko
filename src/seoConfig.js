import { createContext } from 'react';

export const SITE_URL = 'https://uzdrowisko-marki.pl';
export const DEFAULT_IMAGE = `${SITE_URL}/og-uzdrowisko.webp`;
export const SeoCollectorContext = createContext(null);
